" do not use spaces since `git config` puts tabs into .gitconfig files. We
" don't want to have mix of tabs and spaces later.
setlocal noexpandtab

setlocal tabstop=2
setlocal shiftwidth=2
setlocal softtabstop=0
